{
  "name": "news-getter-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --experimental-strip-types --test 'test/**/*.test.ts'"
  },
  "dependencies": {
    "astro": ">=7.0",
    "@astrojs/rss": ">=4.0",
    "@astrojs/check": ">=0.9",
    "typescript": ">=5.0"
  },
  "devDependencies": {
    "@types/node": ">=22.0"
  }
}